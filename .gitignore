node_modules/
.yarn/install-state.gz
.yarn/unplugged/
dist/
build/
*.tsbuildinfo

# Only caches are safely ignorable. Config, normalized report inputs, and
# generated architectural metadata are independently source-controllable
# (see issue #1), so the .topo/ workspace is NOT ignored wholesale.
.topo/cache/

.DS_Store
*.log
.env
.env.*
!.env.example
