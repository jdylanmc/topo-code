node_modules/
.yarn/install-state.gz
.yarn/unplugged/
dist/
build/
*.tsbuildinfo

# Keep the root policy cache-only. This repository's checked-in workspace policy
# separately keeps generated maps and report outputs local while preserving
# config, authored metadata, and normalized report inputs for source control.
.topo/cache/
.skill-log/

.DS_Store
*.log
.env
.env.*
!.env.example
